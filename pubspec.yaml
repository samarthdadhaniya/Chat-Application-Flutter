name: flutter_chat_app
description: A Flutter chat application simulator.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  uuid: ^3.0.7
  intl: ^0.18.1
  shared_preferences: ^2.2.0
  email_validator: ^2.1.17
  flutter_secure_storage: ^8.0.0
  image_picker: ^1.0.4
  cached_network_image: ^3.2.3
  flutter_animate: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
